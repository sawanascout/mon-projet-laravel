[phases.setup]
nixPkgs = ["php81", "composer", "nginx", "nodejs"]

[phases.install]
cmds = ["composer install --no-interaction --prefer-dist --optimize-autoloader"]

[phases.build]
cmds = ["npm install", "npm run build"]

[phases.start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
